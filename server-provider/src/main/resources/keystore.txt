The keystore bundled with HTH is used as default
keystore when HTTPS access is required. Still, you
can define your own keystore, this one is handy
in case you dont care about material but essentially
only to have HTTPS.
